---
phase: 03-developer-experience-polish
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/commands/list.rs
  - src/commands/status.rs
  - src/commands/env.rs
autonomous: true
requirements: [DEVX-02]

must_haves:
  truths:
    - "User can run `clmux list --json` and get a JSON array of profiles"
    - "User can run `clmux status --json` and get a JSON object with profile info"
    - "User can run `clmux env --json` and get a JSON object with env vars"
    - "JSON output is valid and parseable by jq"
    - "Non-JSON output is unchanged from before"
  artifacts:
    - path: "src/commands/list.rs"
      provides: "JSON output for list command"
      contains: "serde_json"
    - path: "src/commands/status.rs"
      provides: "JSON output for status command"
      contains: "serde_json"
    - path: "src/commands/env.rs"
      provides: "JSON output for env command"
      contains: "serde_json"
  key_links:
    - from: "src/commands/list.rs"
      to: "serde_json::to_string_pretty"
      via: "JSON serialization"
      pattern: "serde_json"
    - from: "src/commands/status.rs"
      to: "serde_json::to_string_pretty"
      via: "JSON serialization"
      pattern: "serde_json"
    - from: "src/commands/env.rs"
      to: "serde_json::to_string_pretty"
      via: "JSON serialization"
      pattern: "serde_json"
---

<objective>
Implement machine-parseable JSON output for list, status, and env commands.

Purpose: Enable scripting and automation with structured output (DEVX-02). Users pipe `--json` output to jq or consume it programmatically.
Output: Three command files updated with JSON output paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-developer-experience-polish/03-CONTEXT.md
@.planning/phases/03-developer-experience-polish/03-01-SUMMARY.md

<interfaces>
<!-- From 03-CONTEXT.md decisions — JSON schemas to implement -->

list --json: array of objects
```json
[{"name": "work", "active": true, "path": "/home/user/.clmux/profiles/work"}, ...]
```

status --json: single object
```json
{"profile": "work", "path": "/home/user/.clmux/profiles/work", "exists": true, "config_dir": "/home/user/.clmux/profiles/work", "items": 3}
```

env --json: single object
```json
{"CLAUDE_CONFIG_DIR": "/home/user/.clmux/profiles/work", "CLMUX_PROFILE": "work"}
```

<!-- Existing types the executor needs -->

From src/config.rs:
```rust
#[derive(Serialize, Deserialize, Default)]
pub struct Config {
    pub profiles: Vec<ProfileEntry>,
}

#[derive(Serialize, Deserialize, Clone)]
pub struct ProfileEntry {
    pub name: String,
}
```

From src/state.rs:
```rust
#[derive(Serialize, Deserialize, Default)]
pub struct State {
    pub active: Option<String>,
}
```

From src/paths.rs:
```rust
pub fn profile_dir(name: &str) -> Result<PathBuf>;
```

From src/commands/run.rs (used by env.rs):
```rust
pub fn resolve_profile(name: Option<&str>) -> Result<(String, PathBuf)>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement --json for list and status commands</name>
  <files>src/commands/list.rs, src/commands/status.rs</files>
  <read_first>src/commands/list.rs, src/commands/status.rs, src/config.rs, src/state.rs, src/paths.rs</read_first>
  <action>
1. In src/commands/list.rs, update execute(json: bool):
   - Rename `_json` to `json` (remove underscore).
   - Add `use serde_json::json;` at the top.
   - When `json` is true: build a JSON array where each entry is `{"name": "...", "active": true/false, "path": "/..."}`. Use `paths::profile_dir(&profile.name)?` to get the path. Print via `println!("{}", serde_json::to_string_pretty(&entries)?)` and return early.
   - When `json` is false: existing colored output unchanged.
   - The JSON array structure: `[{"name": "work", "active": true, "path": "/home/user/.clmux/profiles/work"}]`

2. In src/commands/status.rs, update execute(json: bool):
   - Rename `_json` to `json` (remove underscore).
   - Add `use serde_json::json;` at the top.
   - When `json` is true: build a JSON object with fields: `profile` (string), `path` (string), `exists` (bool), `config_dir` (string, same as path -- this is what CLAUDE_CONFIG_DIR would be set to), `items` (number, 0 if dir missing). Print via `serde_json::to_string_pretty` and return early.
   - When `json` is false: existing colored output unchanged.
   - The JSON structure: `{"profile": "work", "path": "...", "exists": true, "config_dir": "...", "items": 3}`
  </action>
  <verify>
    <automated>cd /Users/jose/projects/tepuii-tech/clmux && cargo build 2>&1 && echo '--- list --json ---' && ./target/debug/clmux list --json 2>&1 && echo '--- status --json ---' && ./target/debug/clmux status --json 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - src/commands/list.rs contains `serde_json` import
    - src/commands/list.rs json branch produces array with `name`, `active`, `path` fields
    - src/commands/status.rs contains `serde_json` import
    - src/commands/status.rs json branch produces object with `profile`, `path`, `exists`, `config_dir`, `items` fields
    - `cargo build` succeeds
    - `clmux list --json` output is valid JSON (parseable by jq)
    - `clmux list` (without --json) still shows colored output unchanged
    - `clmux status --json` output is valid JSON
    - `clmux status` (without --json) still shows colored output unchanged
  </acceptance_criteria>
  <done>list --json outputs JSON array of profiles with name/active/path. status --json outputs JSON object with profile/path/exists/config_dir/items. Non-JSON output unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Implement --json for env command and run integration tests</name>
  <files>src/commands/env.rs</files>
  <read_first>src/commands/env.rs, src/commands/run.rs</read_first>
  <action>
1. In src/commands/env.rs, update execute(json: bool):
   - Rename `_json` to `json` (remove underscore).
   - Add `use serde_json::json;` at the top.
   - When `json` is true: build a JSON object with `CLAUDE_CONFIG_DIR` (the profile dir path as string) and `CLMUX_PROFILE` (the profile name). Print via `serde_json::to_string_pretty` and return early. Do NOT include the unset vars -- JSON consumers handle their own env.
   - When `json` is false: existing shell export output unchanged.
   - The JSON structure: `{"CLAUDE_CONFIG_DIR": "/home/user/.clmux/profiles/work", "CLMUX_PROFILE": "work"}`

2. Verify all three commands work end-to-end:
   - `clmux list --json | jq .` succeeds
   - `clmux status --json | jq .` succeeds
   - `clmux env --json | jq .` succeeds
   - All three without --json produce original output
  </action>
  <verify>
    <automated>cd /Users/jose/projects/tepuii-tech/clmux && cargo build 2>&1 && cargo test 2>&1 && echo '--- env --json ---' && ./target/debug/clmux env --json 2>&1 && echo '--- env plain ---' && ./target/debug/clmux env 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - src/commands/env.rs contains `serde_json` import
    - src/commands/env.rs json branch produces object with `CLAUDE_CONFIG_DIR` and `CLMUX_PROFILE` fields
    - `cargo build` succeeds with no warnings
    - `cargo test` passes (all existing tests still work)
    - `clmux env --json` output is valid JSON
    - `clmux env` (without --json) still outputs shell export lines
  </acceptance_criteria>
  <done>env --json outputs JSON object with CLAUDE_CONFIG_DIR and CLMUX_PROFILE. All three commands support --json. All existing tests pass. Non-JSON output unchanged.</done>
</task>

</tasks>

<verification>
- `cargo test` passes
- `cargo build` succeeds with no warnings
- `clmux list --json` outputs valid JSON array with name/active/path per profile
- `clmux status --json` outputs valid JSON object with profile/path/exists/config_dir/items
- `clmux env --json` outputs valid JSON object with CLAUDE_CONFIG_DIR/CLMUX_PROFILE
- `clmux list` (no flag) shows colored output as before
- `clmux status` (no flag) shows colored output as before
- `clmux env` (no flag) shows shell exports as before
</verification>

<success_criteria>
All three read commands (list, status, env) support --json flag producing valid, parseable JSON output matching the schemas defined in CONTEXT.md. Existing non-JSON behavior is completely unchanged. All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-developer-experience-polish/03-02-SUMMARY.md`
</output>
