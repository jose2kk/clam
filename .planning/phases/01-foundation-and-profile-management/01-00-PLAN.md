---
phase: 01-foundation-and-profile-management
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements:
  - PROF-01

must_haves:
  truths:
    - "Rust stable toolchain is installed and functional on this machine"
    - "`rustc --version` and `cargo --version` both exit 0"
  artifacts: []
  key_links: []
---

<objective>
Install the Rust stable toolchain via rustup so that all subsequent plans can compile and test the clmux binary.

Purpose: The Rust compiler is not present on this machine. Every other plan in this phase depends on `cargo build` and `cargo test`, so this must run first.

Output: Working Rust stable toolchain (rustc + cargo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Rust stable toolchain via rustup</name>
  <files></files>
  <action>
    Install Rust using the official rustup installer in non-interactive mode:

    ```bash
    curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
    ```

    After installation, source the cargo environment so it is available in the current session:

    ```bash
    source "$HOME/.cargo/env"
    ```

    Verify both tools are available:

    ```bash
    rustc --version
    cargo --version
    ```

    Both commands must exit 0 and print version strings.
  </action>
  <verify>
    <automated>source "$HOME/.cargo/env" 2>/dev/null; rustc --version && cargo --version</automated>
  </verify>
  <done>rustc and cargo are installed and on PATH. Both `rustc --version` and `cargo --version` exit 0.</done>
</task>

</tasks>

<verification>
- `rustc --version` exits 0 and prints a stable version
- `cargo --version` exits 0
- `which rustc` points to `~/.cargo/bin/rustc`
</verification>

<success_criteria>
- Rust stable toolchain installed
- rustc and cargo available on PATH
- Ready for `cargo init` and `cargo build` in subsequent plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-profile-management/01-00-SUMMARY.md`
</output>
